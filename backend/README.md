# Employee & Task Management System

This repository contains the backend API for the Employee & Task Management System internship project.

Phase 1: Project skeleton created with:

- `EmployeeTaskManagement.csproj`
- `Program.cs`
- `appsettings.json` / `appsettings.Development.json`
- minimal `HealthController` (API health endpoint)

Next steps (ask before proceeding):

1. Configure SQL Server & EF Core
2. Add domain entities and DbContext
3. Implement authentication (JWT)

Run locally:

```bash
dotnet restore
dotnet build
dotnet run
```
